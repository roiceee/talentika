
# Backend URL — used by openapi-ts codegen and server-side BFF routes
BACKEND_URL=http://localhost:8000

# Google Maps JavaScript API key (requires Places API enabled in Google Cloud Console)
# Used for address autocomplete on the public job application form (/jobs/[jobId]).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
